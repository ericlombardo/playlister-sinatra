<!DOCTYPE html>
<html>
  <head>
 
  </head>
  <body>
    <% @genres.artists.each do |a| %>
      <a href="/artists/<%=a.slug%>"><%= a.name %></a>
    <% end %>

    <% @genres.songs.each do |s| %>
      <a href="/songs/<%=s.slug%>"><%= s.name %></a>
    <% end %>
  </body>
</html>